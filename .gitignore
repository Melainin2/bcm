# === Secrets — NE JAMAIS COMMITTER ===
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example
frontend/.env
frontend/.env.*
!frontend/.env.example
*.key
*.pem

# === Python ===
__pycache__/
*.pyc
*.egg-info/
venv/
.venv/
backend/venv/
backend/.pytest_cache/
.pytest_cache/
backend/.cache/

# === Node / frontend ===
node_modules/
frontend/node_modules/
dist/
build/
frontend/dist/

# === ChromaDB (base vectorielle générée — JAMAIS committer, régénérée au build) ===
chroma_db/
chroma_db*/
*.sqlite3

# NB : les PDF de data/ (~72 Mo, < 100 Mo) SONT versionnés : Render en a besoin
# au build pour lancer `python scripts/ingest.py --reset` et générer ChromaDB.

# === OS / éditeurs ===
.DS_Store
.vscode/
.idea/
