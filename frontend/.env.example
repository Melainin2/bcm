# URL du backend FastAPI (déployé séparément : Render / Railway / VPS).
# En local : http://localhost:8000 (ou le port de votre backend).
# En production Vercel : définissez cette variable dans les settings du projet,
#   ex. VITE_API_BASE_URL=https://dba-gpt-backend.onrender.com
VITE_API_BASE_URL=http://localhost:8000
